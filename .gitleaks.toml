# Gitleaks config for Trovara Shop CI.
# Extends defaults; allowlists known false positives (not real secrets).

title = "trovara-shop"

[extend]
useDefault = true

[allowlist]
description = "Known non-secret theme namespace; scan documentation and tests"
regexes = [
  '''^trovara_shop_theme$''',
]
